COMO SUBIR

git init

git remote --v

git checkout 'branch'

git status

git add .

git commit -m 'mensagem'

git push origin main

'talvez de erro de pull mas é so fazer'

git log 

git reset --hard 'hash'
